using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerNavigation : MonoBehaviour
{
    
    void changeLocation(int currentLocation)
    {
        currentLocation = 0;
        switch(currentLocation){

            case 0:
                currentLocation = 0;
            break;
            

        }
    }
}
